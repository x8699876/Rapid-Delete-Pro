1. build the project both console and UI version in the IDE.
2. run the maven package. it will copy the jars to the dist directory.
3. for MAC, run the ant build to package the rdpro-ui.jar into the  the Mac App.